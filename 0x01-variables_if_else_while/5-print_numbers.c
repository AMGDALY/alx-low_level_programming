#include <stdio.h>
#include <stdlib.h>

/**
 *main - Entry pint
 *Discription: 'Check for number if it is positive or negative'
 *Return: always 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
	printf("%d", i);
	}
	printf(" ");
	return (0);
}
